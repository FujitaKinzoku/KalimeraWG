# FRONT: CDN-фронтированный VLESS-релей

## Статус этапа

> **Полевой тест подтверждён 23 августа 2026 года.** На тестовой мобильной
> сети с активным режимом белых списков была установлена новая сессия по
> маршруту **совместимый облачный CDN → FRONT → ENTRY**. После входа в ENTRY
> сохранилось штатное разделение: RU-трафик направлялся через SOCKS5/TUN
> либо fail-open ENTRY, остальной - через межсерверный AWG 3+ на EXIT.

Это подтверждает работоспособность **концепции и конкретной развёрнутой
цепочки**. Результат не означает, что любой адрес выбранного CDN-провайдера,
любой CDN edge или тот же профиль будут доступны у каждого оператора и во всех
регионах.
Allowlist - внешняя динамическая политика, которой KalimeraWG не управляет.

| Зафиксировано этим этапом | Статус |
|---|---|
| новое соединение во время активного ограничения | подтверждено |
| клиент → доступный CDN edge | подтверждено на тестовой сети |
| CDN → FRONT по HTTPS | подтверждено |
| FRONT → ENTRY по VLESS+REALITY | подтверждено |
| прежние RU/EXIT-маршруты после ENTRY | подтверждено |
| одинаковая работа у всех операторов/регионов | не заявляется |
| автоматический выбор между несколькими CDN/FRONT | будущий этап |

## Зачем нужен FRONT

При обычной DPI-фильтрации маскировка протокола может быть достаточной. При
строгом IP allowlist пакеты к недоступному IP отбрасываются раньше, чем
REALITY, SNI или параметры AmneziaWG смогут повлиять на соединение. Поэтому
прямой ENTRY остаётся быстрым основным входом, но для ограниченного режима
появляется отдельная доступная точка входа - CDN.

FRONT - отдельная VPS с Nginx и Xray-core за **совместимым облачным CDN**. Клиент
устанавливает обычный TLS к клиентскому домену CDN и передаёт VLESS через
XHTTP. CDN доставляет HTTP-поток на FRONT; Xray на FRONT открывает отдельный
VLESS+REALITY-хоп к существующему ENTRY. Клиент CDN-пути не обращается к
публичному IP ENTRY напрямую.

Размещение произвольной VM у облачного провайдера само по себе не делает её
доступной во время ограничений. Проверяется именно конечный клиентский домен и
фактический CDN edge. Этот документ фиксирует успешный тест выбранного ресурса,
а не использует ASN облака как неподвижный «белый диапазон».

## Архитектура

<p align="center">
  <img src="../assets/whitelist-cascade-ru.svg" alt="Три независимых входа в KalimeraWG и общая маршрутизация после ENTRY" width="100%">
</p>

### Технология по слоям

| Участок | Протокол и компонент | Что видит следующая сторона | Назначение |
|---|---|---|---|
| клиент → CDN | TLS 1.3, TCP/443, VLESS+XHTTP | CDN-домен, действительный сертификат и HTTP-поток | доступный внешний вход при проверенном allowlist |
| CDN → FRONT | HTTPS к origin, Nginx | запросы только на случайный XHTTP path | доставка без раскрытия origin клиентскому профилю |
| Nginx → Xray FRONT | локальный HTTP/1.1 с keepalive, без buffering | loopback `127.0.0.1` | передача долгоживущего XHTTP-потока Xray |
| FRONT → ENTRY | VLESS+REALITY, TCP/443 | REALITY endpoint ENTRY | отдельный зашифрованный внутренний хоп |
| ENTRY routing | sing-box sniff + ipset/marks/tables | доменная и сетевая политика | выбор RU либо EXIT без изменения правил каскада |
| ENTRY → EXIT | отдельный AWG 3+ | зашифрованный межсерверный транспорт | основной зарубежный egress |
| ENTRY → RU | SOCKS5/TUN либо WAN fail-open | российский egress | российские домены и сети |

### Последовательность CDN-пути

```text
Клиент
  └─ TLS + VLESS/XHTTP :443
       └─ Совместимый облачный CDN
            └─ HTTPS origin
                 └─ Nginx FRONT
                      └─ Xray FRONT
                           └─ VLESS+REALITY :443
                                └─ sing-box ENTRY
                                     ├─ RU → SOCKS5/TUN или WAN ENTRY
                                     └─ default/se-domain → AWG 3+ → EXIT
```

Каждый защищённый участок заканчивается только там, где это необходимо:
внешний TLS - на CDN/FRONT, внутренний REALITY - на ENTRY, а межсерверный AWG
3+ - на EXIT. CDN не получает ключи AWG и не участвует в маршрутизации после
ENTRY.

### Независимые профили доступа

| Профиль | Когда использовать | Накладные расходы | Зависимость |
|---|---|---:|---|
| AWG 3+ напрямую | обычная сеть, UDP доступен | минимальные | IP ENTRY и UDP/443 |
| VLESS+REALITY напрямую | UDP/AWG фильтруется, TCP/443 доступен | низкие | IP ENTRY и TCP/443 |
| CDN/XHTTP через FRONT | прямой IP ENTRY недоступен, CDN edge доступен | выше из-за CDN и дополнительного хопа | DNS, CDN, FRONT и TCP/443 |

FRONT не заменяет AmneziaWG и не добавляется перед ним. Клиент хранит несколько
профилей и выбирает доступный. После соединения все профили сходятся на ENTRY и
получают одинаковую политику маршрутизации.

Клиенты FRONT используют тот же UUID, что прямой REALITY-профиль
(`reality_fallback_client_uuid`). Отдельный UUID
`reality_fallback_front_client_uuid` предназначен только для хопа
FRONT→ENTRY и конечным пользователям не выдаётся.

## Матрица отказов

| Событие | AWG напрямую | REALITY напрямую | CDN/FRONT | Что остаётся доступно |
|---|---:|---:|---:|---|
| заблокирован UDP | нет | да | да | оба TCP-пути |
| IP ENTRY недоступен клиенту | нет | нет | да, если CDN edge доступен | CDN/FRONT |
| отказ FRONT | да | да | нет | прямые профили |
| отказ CDN/DNS клиентского домена | да | да | нет | прямые профили |
| отказ EXIT | соединение возможно | соединение возможно | соединение возможно | RU/fail-open; default egress нарушен |
| отказ RU SOCKS5 | да | да | да | автоматический fail-open через ENTRY |
| полное отключение передачи данных оператором | нет | нет | нет | сетевого пути не существует |

## Границы полевого подтверждения

Проверка доказывает работоспособность конкретного маршрута, но не позволяет
зафиксировать внешнюю allowlist-политику. Для каждого нового оператора, региона
или CDN-ресурса отдельно проверяются:

1. разрешение клиентского домена системным DNS до запуска туннеля;
2. установление **новой**, а не ранее сохранённой TCP/TLS-сессии;
3. отсутствие прямого обращения клиента к IP FRONT или ENTRY;
4. прохождение XHTTP через CDN без кэширования и buffering;
5. внешний IP для RU и default-направлений;
6. длительная загрузка, выгрузка, простой и повторное подключение;
7. сохранение рабочих AWG/REALITY-профилей при отказе FRONT.

## Что делается вручную (вне Ansible)

Регистрация домена, DNS-записи и ресурс выбранного CDN-провайдера не
автоматизированы - это одноразовая настройка через его консоль или CLI, а
не SSH-based provisioning, которым управляет весь остальной проект.

1. Зарегистрируйте домен (или используйте поддомен уже имеющегося).
2. Поднимите VPS для FRONT у подходящего облачного или VPS-провайдера.
   Репутация ASN этого сервера не критична, потому что
   клиент никогда не обращается к его IP напрямую, только через CDN.
3. Создайте A-запись **origin-домена** (например `origin.example.com`),
   указывающую на публичный IP FRONT. Если DNS-провайдер умеет проксировать
   записи, для origin должна быть выбрана политика **DNS only**: при
   включённом проксировании ACME-проверка Certbot не дойдёт до FRONT, а
   трафик попадёт в постороннюю проксирующую сеть вместо выбранного CDN.
4. Дайте Ansible выпустить сертификат origin-домена (Certbot на самом
   FRONT, HTTP-01) - это происходит при `./deploy --resume`, отдельных
   действий не требует.
5. В консоли выбранного совместимого CDN-провайдера создайте ресурс **CDN**:
   источник - origin-домен из шага 3, протокол до источника - **HTTPS**,
   заголовок Host - тот же origin-домен.
6. Клиентский домен (тот, на который в итоге смотрит клиент) - отдельное
   имя. Для него нужен **свой** сертификат в менеджере сертификатов выбранного
   CDN-провайдера: это не альтернатива Certbot из шага 4, а второй сертификат
   для второго домена. Certbot закрывает origin, менеджер сертификатов -
   клиентскую сторону CDN.
   Можно обойтись без него, если клиент поддерживает технический домен,
   выданный выбранным CDN-провайдером.
7. Получите технический CNAME-домен CDN и добавьте на него публичную
   DNS-запись клиентского домена. Если DNS-провайдер умеет проксировать
   записи, здесь также выберите **DNS only**.
8. **Отключите кэширование для XHTTP-пути** (`front_xhttp_path`) в
   настройках ресурса CDN. Это долгоживущий поток, а не статика: если CDN
   начнёт кэшировать или буферизовать тело ответа, транспорт сломается.

## Подключение через Ansible

FRONT добавляется постфактум, не через начальный мастер (`./deploy`), а
идемпотентной командой - по аналогии с `./deploy --resume --enable-mobile`:

```bash
./deploy --resume --enable-front
```

Команда интерактивно спросит IP/SSH-данные FRONT, origin-домен (шаг 3
выше) и e-mail для Let's Encrypt/Certbot, запишет их в
`inventory/production/group_vars/front.yml` и добавит группу `front` в
`hosts.yml`. После этого выполните обычное применение:

```bash
./deploy --resume
```

Это развернёт Nginx, Certbot (сертификат для origin-домена, HTTP-01), и
Xray-core на FRONT, а также добавит второй REALITY-uuid в `reality-in`
на ENTRY.

## Известные ограничения текущего этапа

- FRONT не проходит через общую роль `security` (жёстко привязана к
  ENTRY/EXIT) - SSH-хардening/смена порта на FRONT не автоматизированы,
  используется то, что уже настроено на сервере оператором.
- FRONT не участвует в пороговом (Shamir 2-of-5) хранении секретов -
  на нём нет ключей AmneziaWG, только TLS-сертификат и REALITY-uuid.
- `playbooks/front.yml` не включает `preflight` (эта роль жёстко
  проверяет пару ровно ENTRY+EXIT) - синтаксические проверки Ubuntu/WAN
  для FRONT не выполняются.
- Nginx/Xray FRONT пока не приведены к полной no-logs/runtime-secrets модели
  ENTRY и EXIT. До отдельного пакета hardening FRONT следует считать
  самостоятельной доверенной границей.
- Ручная команда `ru-proxy off` останавливает общий процесс sing-box, который
  обслуживает не только RU SOCKS5, но и `reality-in`. До разделения служб это
  одновременно отключает прямой REALITY и внутренний хоп FRONT→ENTRY;
  автоматический fail-open, не останавливающий sing-box, этой проблемы не имеет.
- Автоматической балансировки между несколькими FRONT/CDN нет. Клиент должен
  иметь прямые и CDN-профили заранее, пока обычная сеть доступна.
- Короткая ссылка `vless://` не переносит вложенный `xhttpSettings.extra`.
  Расширенная маскировка требует полного клиентского JSON и проверки
  совпадения XHTTP-настроек клиента и сервера.

## Актуальность версии Xray-core

`front_xray_version`/`front_xray_packages` - это версия **сервера**
(FRONT). Клиентские приложения (Happ и другие) обновляют встроенный
Xray-core по своему расписанию, независимо от этого проекта, и версии
расходятся сами собой со временем - это не разовая настройка, а то, что
требует периодической проверки.

Расхождение версий - не косметика. Между v26.3.27 (было закреплено
здесь) и v26.7.28 (то, что реально бандлит Happ v5.6.0) на практике
разошлось поведение самого протокола XHTTP: валидация пути (завершающий
слэш) и согласование режима `packet-up`/`stream-one` - оба случая
воспроизведены локально парой реальных бинарников клиент+сервер и оба
проявлялись как необъяснимый 404/обрыв сессии у живого клиента при
абсолютно корректном конфиге. Гнаться за этим через правки JSON
бессмысленно, если причина - версия.

**Как проверить, не отстала ли закреплённая версия:**

1. Версию, которую реально использует клиент, проще всего узнать из его
   же логов подключения (в Happ - экспорт логов/диагностика в настройках;
   первая строка лога Xray-core - `Xray <версия> started`).
2. Сравнить с `front_xray_version` в `roles/front/defaults/main.yml`.

**Как проверить кандидата на обновление, прежде чем менять прод** (без
единого захода на живой каскад - локально, парой реальных бинарников на
любой Linux/WSL-машине):

```bash
curl -fsSL -o xray.zip https://github.com/XTLS/Xray-core/releases/download/<версия>/Xray-linux-64.zip
sha256sum xray.zip   # зафиксировать в defaults/main.yml как checksum
unzip xray.zip -d extracted   # или python -c "import zipfile; zipfile.ZipFile('xray.zip').extractall('extracted')", если unzip недоступен
./extracted/xray vlessenc   # формат вывода должен остаться "mlkem768x25519plus.native.0rtt.<key>" -
                             # именно его парсит roles/front/tasks/main.yml и tests/check-front.yml
./extracted/xray x25519     # метка должна остаться "Password (PublicKey): <key>"
```

Затем - реальный раунд-трип клиент+сервер на кандидате (не только
`xray -test`, который проверяет только схему JSON, а не фактическое
поведение сессии): поднять два процесса `xray run` локально с одинаковым
`xhttpSettings.extra` (тем, что в `xray-config.json.j2`), один как VLESS-
inbound на 127.0.0.1, другой как outbound с socks-инбаундом, и прогнать
`curl -x socks5h://127.0.0.1:<port> http://example.com` через получившийся
туннель. Именно так был пойман и подтверждён баг 2026-08-22 - быстрее и
надёжнее, чем каждый раз проверять через живой телефон.

**Не переключать `front_xray_version` на "всегда последнюю" автоматически** -
это убирает контроль через закреплённый checksum и может уронить прод
непроверенным breaking change при обычном `./deploy --resume`. Обновление
версии - сознательное решение с проверкой выше, а не фоновая задача.

## Получение клиентской ссылки

Клиентская vless-ссылка для FRONT использует **тот же** uuid, что и
обычный прямой REALITY (`reality_fallback_client_uuid`), но:

- адрес - клиентский домен CDN (шаг 7 выше), порт 443;
- транспорт - `xhttp`, путь - значение `front_xhttp_path` из
  `group_vars/front.yml` (генерируется случайным при `--enable-front`,
  чтобы не давать fingerprint по общеизвестному пути), режим - значение
  `front_xhttp_mode` (по умолчанию `packet-up`);
- TLS - обычный (не REALITY) с реальным сертификатом, SNI = тот же
  клиентский домен;
- `flow` в клиентской ссылке НЕ указывается: XTLS Vision работает только
  поверх сырого TCP и с XHTTP несовместим.

Например:

```
vless://<uuid>@cdn.example.com:443?encryption=none&security=tls&sni=cdn.example.com&type=xhttp&path=%2F<front_xhttp_path>&mode=packet-up#FRONT
```

Второй uuid (`reality_fallback_front_client_uuid`) НЕ используется в
клиентских ссылках - он только для соединения FRONT→ENTRY.

## Настройки, которые может понадобиться менять

Обе живут в `inventory/production/group_vars/front.yml` и применяются
обычным `./deploy --resume`:

- `front_xhttp_mode` (`packet-up` | `stream-up` | `stream-one` | `auto`).
  В текущем подтверждённом сценарии `packet-up` использует
  `"uplinkHTTPMethod": "GET"`, что соответствует разрешённым по умолчанию
  методам выбранного CDN-ресурса. Это должно быть явно одинаково задано на
  клиенте и сервере. Остальные режимы обычно используют POST и требуют, чтобы
  CDN разрешал метод и не буферизовал тело запроса. Xray допускает GET только
  в `packet-up`.
- `front_reality_flow` (`xtls-rprx-vision-udp443` | `xtls-rprx-vision`) -
  flow на внутреннем хопе FRONT→ENTRY. По умолчанию `-udp443`, и менять
  это почти наверняка не нужно: обычный `xtls-rprx-vision` в Xray
  **перехватывает UDP/443 (QUIC)**. Для клиента на самом устройстве это
  безвредно (приложение мгновенно откатывается на TCP), но FRONT стоит
  посередине, и до приложения отказ не доходит - оно ждёт таймаута. Так
  ломается воспроизведение YouTube при полностью рабочих Telegram и
  обычных сайтах. В sing-box на ENTRY `xtls-rprx-vision` соответствует
  именно варианту `-udp443` и UDP/443 не блокирует.
- `front_vless_encryption_enabled` (`false` по умолчанию) - независимый
  от TLS слой шифрования самого VLESS (ML-KEM-768 + X25519, PFS +
  anti-replay 0-RTT; XTLS/Xray-core PR #5067). При включении роль один раз
  генерирует пару `xray vlessenc` (`front_vless_decryption_path`/
  `front_vless_encryption_path` в `front_xray_state_dir`, идемпотентно -
  тем же приёмом, что ключи REALITY). Значение из
  `front_vless_encryption_path` подставляется в клиентскую ссылку вместо
  `encryption=none`; узнать его -
  `cat front_vless_encryption_path` на FRONT.
- `front_xhttp_disguise_enabled` (`false` по умолчанию) - маскирует XHTTP
  под конкретное веб-приложение, а не просто случайный путь: id сессии в
  cookie (`front_xhttp_disguise_session_cookie`, по умолчанию `PHPESSID`),
  аплинк в теле запроса под именем рекламного параметра
  (`front_xhttp_disguise_uplink_param`, по умолчанию `_dc`), паддинг под
  токен (`front_xhttp_disguise_padding_key`, по умолчанию `mc_token`).
  **Не выражается в короткой ссылке `vless://`** - вложенный объект
  `extra` не помещается в query-параметры, клиенту нужен полный JSON-конфиг
  outbound'а (см. `xray vlessenc`/`xhttpSettings.extra` в документации
  Xray). Это меняет процесс выдачи конфигурации: вместо ссылки -
  файл конфигурации.

## Подписка (обновление конфигурации без переустановки)

`front_subscription_enabled` (`false` по умолчанию) поднимает на FRONT
HTTP-подписку - Happ и совместимые клиенты периодически перезапрашивают
её сами и подтягивают изменения адреса/пути/режима/шифрования без
ручного пересоздания профиля. Подтверждено официальной документацией Happ
(`happ.su/main/dev-docs/app-management`): тело подписки - обычный список
ссылок `vless://` в base64, метаданные передаются через HTTP-заголовки
(`profile-title`, `profile-update-interval`), а не JSON - поэтому
**маскировка XHTTP (`front_xhttp_disguise_enabled`) через подписку
недоступна принципиально**, только через отдельный ручной JSON-импорт.
Ссылка в подписке всегда без `extra`, но с VLESS Encryption, если он
включён (`encryption=` - обычный query-параметр, в отличие от `extra`).

Настройки:
- `front_subscription_path` - публичный, ничем не защищённый URL. При
  включении **обязательно** переопределите его в `group_vars/front.yml`
  на непубличный (тот же принцип, что и `front_xhttp_path`).
- `front_client_domain` - домен в самой ссылке подписки; по умолчанию
  равен `front_origin_domain`. После подключения CDN достаточно
  переопределить только эту переменную и повторить `./deploy --resume` -
  подписка обновится сама, у клиента отдельных действий не потребуется.
- `front_subscription_title` - имя профиля в приложении (заголовок
  `profile-title`, обязан быть ASCII - HTTP-заголовки не поддерживают
  произвольную кодировку без дополнительного base64).

Ссылка на подписку для добавления в Happ:
`https://<front_client_domain><front_subscription_path>`.
