---
- name: Перезапуск межсерверного AWG 3+
  ansible.builtin.systemd_service:
    name: "{{ awg3_transit_service_name }}"
    state: restarted
    daemon_reload: true
  when: awg3_transit_enabled | bool
